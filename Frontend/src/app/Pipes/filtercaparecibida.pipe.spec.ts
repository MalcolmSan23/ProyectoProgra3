import { FiltercaparecibidaPipe } from './filtercaparecibida.pipe';

describe('FiltercaparecibidaPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltercaparecibidaPipe();
    expect(pipe).toBeTruthy();
  });
});
