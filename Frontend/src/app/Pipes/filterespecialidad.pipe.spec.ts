import { FilterespecialidadPipe } from './filterespecialidad.pipe';

describe('FilterespecialidadPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterespecialidadPipe();
    expect(pipe).toBeTruthy();
  });
});
