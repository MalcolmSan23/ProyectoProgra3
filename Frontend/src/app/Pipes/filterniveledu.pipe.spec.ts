import { FilterniveleduPipe } from './filterniveledu.pipe';

describe('FilterniveleduPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterniveleduPipe();
    expect(pipe).toBeTruthy();
  });
});
