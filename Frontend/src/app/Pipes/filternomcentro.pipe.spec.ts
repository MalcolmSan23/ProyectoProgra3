import { FilternomcentroPipe } from './filternomcentro.pipe';

describe('FilternomcentroPipe', () => {
  it('create an instance', () => {
    const pipe = new FilternomcentroPipe();
    expect(pipe).toBeTruthy();
  });
});
