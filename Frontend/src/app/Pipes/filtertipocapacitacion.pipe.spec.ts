import { FiltertipocapacitacionPipe } from './filtertipocapacitacion.pipe';

describe('FiltertipocapacitacionPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltertipocapacitacionPipe();
    expect(pipe).toBeTruthy();
  });
});
