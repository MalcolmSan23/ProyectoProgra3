import { FiltertiponiveleduPipe } from './filtertiponiveledu.pipe';

describe('FiltertiponiveleduPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltertiponiveleduPipe();
    expect(pipe).toBeTruthy();
  });
});
