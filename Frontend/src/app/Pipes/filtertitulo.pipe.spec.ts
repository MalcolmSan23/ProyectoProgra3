import { FiltertituloPipe } from './filtertitulo.pipe';

describe('FiltertituloPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltertituloPipe();
    expect(pipe).toBeTruthy();
  });
});
